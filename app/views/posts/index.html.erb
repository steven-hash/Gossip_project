<h1> Potin </h1>

    <%=@posts.each do |post|%>

    <h2><%= post.title %></h2>
    <p><%= post.content %></p>


<p>
<a href="<%= post_path(post.id)%>" > 
</p>
<% end %>

<p>

<a href="<%= new_post_path %>"

</p>
